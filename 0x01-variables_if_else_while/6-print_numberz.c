#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - prints Numbers
* Return: Always 0 (Success)
*/

int main(void)
{
int a;
for (a = '0'; a <= '0'; a++)
putchar(a);
putchar('\n');
return (0);
}
